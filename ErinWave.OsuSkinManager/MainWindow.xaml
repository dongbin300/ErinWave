<Window x:Class="ErinWave.OsuSkinManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ErinWave.OsuSkinManager"
        mc:Ignorable="d"
        Title="ErinWave osu! Skin Manager" Height="700" Width="1200"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#FF0D1117">

    <Window.Resources>
        <!-- Global Color Palette -->
        <SolidColorBrush x:Key="PrimaryBackground" Color="#FF0D1117"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FF161B22"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF30363D"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF0969DA"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF3FB950"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#FFFB8500"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFF0F6FC"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FF8B949E"/>
        <SolidColorBrush x:Key="TextMuted" Color="#FF484F58"/>

        <!-- 버튼 스타일 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1F6EB7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF1654A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1C2128"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF262C36"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox 스타일 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FF0D1117"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBlue}"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card 스타일 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
        </Style>

        <!-- Header Text 스타일 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <!-- Subheader Text 스타일 -->
        <Style x:Key="SubheaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        </Style>

        <!-- Body Text 스타일 -->
        <Style x:Key="BodyTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 상단 제목 표시줄 -->
        <Border Grid.Row="0" Height="72" Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 왼쪽: 앱 제목 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="32" Height="32" Fill="{StaticResource AccentBlue}" Margin="0,0,16,0"/>
                    <StackPanel>
                        <TextBlock Text="ErinWave osu! Skin Manager"
                                  Style="{StaticResource HeaderTextStyle}"/>
                        <TextBlock Text="스킨 관리 및 이미지 편집 도구"
                                  Style="{StaticResource SubheaderTextStyle}" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <!-- 오른쪽: 액션 버튼 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                          HorizontalAlignment="Right">
                    <Button Style="{StaticResource SecondaryButtonStyle}"
                           Name="RefreshButton" Content="🔄 새로고침"
                           Click="RefreshButton_Click"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}"
                           Name="CopySkinButton" Content="📋 스킨 복사"
                           Click="CopySkinButton_Click"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}"
                           Name="DeleteSkinButton" Content="🗑️ 스킨 삭제"
                           Click="DeleteSkinButton_Click"/>
                    <Button Style="{StaticResource PrimaryButtonStyle}"
                           Name="ImageEditorButton" Content="🎨 이미지 편집기"
                           Click="ImageEditorButton_Click" Margin="16,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 메인 콘텐츠 -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 왼쪽 패널 - 스킨 목록 -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 제목 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{StaticResource AccentBlue}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock Text="스킨 목록" Style="{StaticResource HeaderTextStyle}" FontSize="16"/>
                    </StackPanel>

                    <!-- 검색 상자 -->
                    <TextBox Grid.Row="2" Name="SearchTextBox"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Text="🔍 스킨 검색..."
                             Foreground="{StaticResource TextMuted}"
                             Height="44"
                             TextChanged="SearchTextBox_TextChanged"/>

                    <!-- 스킨 리스트 -->
                    <ListBox Grid.Row="4" Name="SkinsListView"
                             Background="Transparent"
                             BorderThickness="1"
                             BorderBrush="{StaticResource BorderBrush}"
                             Foreground="{StaticResource TextPrimary}"
                             SelectionMode="Single"
                             SelectionChanged="SkinsListView_SelectionChanged"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             Padding="8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,8" CornerRadius="4" Background="Transparent">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Grid.Column="0" Width="24" Height="24" Fill="{StaticResource AccentGreen}" Margin="0,0,12,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource BodyTextStyle}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 오른쪽 패널 - 스킨 정보 -->
            <TabControl Grid.Column="2" Background="Transparent">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Margin" Value="2,0,2,0"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Background="#FF3F3F46"
                                           CornerRadius="6,6,0,0" BorderThickness="0">
                                        <ContentPresenter x:Name="ContentSite"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center"
                                                        ContentSource="Header"
                                                        Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FF0078D4"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FF505050"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="📋 정보">
                    <Border Style="{StaticResource CardStyle}" Margin="0,10,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <!-- 스킨 정보 헤더 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                    <TextBlock Text="ℹ️" FontSize="20" Margin="0,0,10,0" Foreground="#FF0078D4"/>
                                    <TextBlock Text="스킨 정보" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                                </StackPanel>

                                <!-- 기본 정보 그리드 -->
                                <Grid Margin="0,0,0,25">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="스킨 이름:" FontWeight="SemiBold"
                                             Foreground="#FF0078D4" Margin="0,0,0,8"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Name="SkinNameTextBlock"
                                             Text="선택된 스킨 없음" Foreground="White" Margin="0,0,0,8"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="제작자:" FontWeight="SemiBold"
                                             Foreground="#FF0078D4" Margin="0,0,0,8"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Name="AuthorTextBlock"
                                             Text="-" Foreground="White" Margin="0,0,0,8"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="버전:" FontWeight="SemiBold"
                                             Foreground="#FF0078D4" Margin="0,0,0,8"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Name="VersionTextBlock"
                                             Text="-" Foreground="White" Margin="0,0,0,8"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="경로:" FontWeight="SemiBold"
                                             Foreground="#FF0078D4" Margin="0,0,0,8"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" Name="PathTextBlock"
                                             Text="-" Foreground="White" Margin="0,0,0,8" TextWrapping="Wrap"/>
                                </Grid>

                                <!-- 미리보기 -->
                                <StackPanel>
                                    <TextBlock Text="미리보기:" FontWeight="SemiBold"
                                             Foreground="#FF0078D4" Margin="0,0,0,10"/>
                                    <Border CornerRadius="8" Background="#FF1E1E1E" Padding="10"
                                           MaxWidth="300" MaxHeight="200" HorizontalAlignment="Left">
                                        <Image Name="PreviewImage" Stretch="Uniform"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </TabItem>

                <TabItem Header="📁 파일">
                    <Border Style="{StaticResource CardStyle}" Margin="0,10,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 파일 필터 -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <RadioButton Name="AllFilesRadio" Content="📄 전체" IsChecked="True"
                                           Foreground="White" FontSize="14" Margin="0,0,20,0"
                                           Checked="FileFilter_Checked"/>
                                <RadioButton Name="ImagesRadio" Content="🖼️ 이미지"
                                           Foreground="White" FontSize="14" Margin="0,0,20,0"
                                           Checked="FileFilter_Checked"/>
                                <RadioButton Name="AudioRadio" Content="🎵 오디오"
                                           Foreground="White" FontSize="14" Margin="0,0,20,0"
                                           Checked="FileFilter_Checked"/>
                                <RadioButton Name="ConfigRadio" Content="⚙️ 설정"
                                           Foreground="White" FontSize="14"
                                           Checked="FileFilter_Checked"/>
                            </StackPanel>

                            <!-- 파일 목록 -->
                            <ListView Grid.Row="2" Name="FilesListView"
                                     Background="#FF2D2D30" Foreground="White"
                                     BorderThickness="0"
                                     SelectionMode="Single"
                                     MouseDoubleClick="FilesListView_MouseDoubleClick">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="파일명" Width="300" DisplayMemberBinding="{Binding Name}"/>
                                        <GridViewColumn Header="타입" Width="100" DisplayMemberBinding="{Binding Type}"/>
                                        <GridViewColumn Header="크기" Width="120" DisplayMemberBinding="{Binding Size}"/>
                                        <GridViewColumn Header="수정일" Width="150" DisplayMemberBinding="{Binding LastModified}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                    </Border>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- 하단 상태 표시줄 -->
        <Border Grid.Row="2" Height="48" Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 왼쪽: 상태 메시지 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource AccentGreen}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Name="StatusTextBlock" Text="준비"
                             Style="{StaticResource SubheaderTextStyle}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 오른쪽: 정보 텍스트 -->
                <TextBlock Grid.Column="1" Text="ErinWave osu! Skin Manager v1.0"
                         Style="{StaticResource SubheaderTextStyle}" HorizontalAlignment="Right"
                         VerticalAlignment="Center" Opacity="0.7"/>
            </Grid>
        </Border>
    </Grid>
</Window>
